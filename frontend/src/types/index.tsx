export interface ImageBlock {
  title: string;
  subtitle: string;
  filename: string;
  mimetype: string;
  index: number;
}

export interface TextBlock {
  title: string;
  body: string;
  index: number;
  subtitle: string;
}
